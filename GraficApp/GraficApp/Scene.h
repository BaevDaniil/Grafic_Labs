#pragma once

/*#include "framework.h"
#include "Shape.h"
#include "SkyBox.h"
#include "camera.h"

#include <vector>

class Scene
{
public:
    //static Scene& GetInstance();
    //Scene();
    Scene(const Scene&) = delete;
    Scene(Scene&&) = delete;

    //~Scene();

    HRESULT InitScene(ID3D11Device* pDevice);
    HRESULT UpdateScene();

    void AddShape(Shape shape, Position pos);
private:
    std::vector<Shape> shapes;
    std::vector<SkyBox> skybox;
    Camera* pCamera_ = nullptr;
};*/

